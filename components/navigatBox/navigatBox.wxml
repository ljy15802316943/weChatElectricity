<!--components/navigatBox/navigatBox.wxml-->

<view class="navBox">
  <view class="navBoxItem"
    wx:for="{{navgetList}}"
    wx:key="goods_id"
    bind:tap="handleNavItem"
  >
    <image class="" src="{{item.image_src}}" mode=""></image>
  </view>
</view>